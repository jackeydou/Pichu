{
  "name": "@pichu/sites-plugin",
  "version": "1.0.10",
  "description": "Bundled Pichu Sites plugin source package.",
  "private": true,
  "type": "module",
  "files": [
    "plugin",
    "scripts"
  ],
  "scripts": {
    "validate:plugin": "node ../../apps/pichu-client/scripts/validate-plugin.mjs plugin",
    "validate:template": "node scripts/validate-template.mjs",
    "test": "pnpm run validate:plugin && pnpm run validate:template"
  },
  "engines": {
    "node": ">=20"
  }
}
