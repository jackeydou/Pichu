{
  "name": "@pichu/mac-cursor-overlay",
  "version": "0.1.0",
  "description": "Native macOS cursor overlay window for Pichu computer use.",
  "private": true,
  "main": "loader.js",
  "types": "index.d.ts",
  "files": [
    "loader.js",
    "index.js",
    "index.d.ts",
    "*.node"
  ],
  "os": [
    "darwin"
  ],
  "engines": {
    "node": ">=18"
  },
  "napi": {
    "binaryName": "mac-cursor-overlay",
    "targets": [
      "aarch64-apple-darwin",
      "x86_64-apple-darwin"
    ]
  },
  "scripts": {
    "build": "napi build --platform --release",
    "build:debug": "napi build --platform",
    "build:universal": "napi build --platform --release --target universal-apple-darwin",
    "prepublishOnly": "napi prepublish --skip-gh-release"
  },
  "devDependencies": {
    "@napi-rs/cli": "^3.6.2"
  }
}
